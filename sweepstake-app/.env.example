# Database (SQLite - no config needed, uses prisma/dev.db)
# DATABASE_URL is configured in prisma.config.ts

# Admin authentication - set a secret key for admin access
# Use this when logging in at /admin
ADMIN_SECRET=your-secret-admin-key-change-me
